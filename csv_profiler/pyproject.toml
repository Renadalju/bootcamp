[project]
name = "csv-profiler"
version = "0.1.0"
description = "A simple CSV profiling tool with CLI and Streamlit UI"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "typer>=0.9.0",
    "streamlit>=1.32.0",
]

[project.scripts]
csv-profiler = "csv_profiler.cli:app"